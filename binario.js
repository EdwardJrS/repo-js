/*
 * Crea un programa se encargue de transformar un número
 * decimal a binario sin utilizar funciones propias del lenguaje que lo hagan directamente.
 */

let numero = 0;

function bionarioConverter (num) {
  console.log(num / 2);
  console.log(0 / 2);
}

bionarioConverter(2);
